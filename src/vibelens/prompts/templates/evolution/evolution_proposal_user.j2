Analyze {{ session_count }} coding agent session{{ "s" if session_count != 1 else "" }} to detect workflow patterns and propose improvements to existing skills.

{{ session_digest }}

## Installed Skills (EXHAUSTIVE — the ONLY evolvable elements)

The list below is the COMPLETE set of skills eligible for evolution. No other skill exists on this user's machine.

{% for skill in installed_skills %}
- [id={{ loop.index0 }}] **{{ skill.name }}**: {{ skill.description }}
{% endfor %}

Provide `title`, workflow patterns with real `example_refs`, and (only when justified) evolution proposals targeting installed skills.
